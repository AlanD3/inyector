import { FloatingDockContainerDirective } from './floating-dock-container.directive';

describe('FloatingDockContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatingDockContainerDirective();
    expect(directive).toBeTruthy();
  });
});
